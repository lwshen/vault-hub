VersionResponse:
  type: object
  required:
    - version
    - commit
  properties:
    version:
      type: string
      description: Application version
      example: "v1.0.0"
    commit:
      type: string
      description: Git commit hash
      example: "abc1234"